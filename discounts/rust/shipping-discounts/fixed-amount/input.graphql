query Input {
  cart {
    deliveryGroups {
      id
    }
  }
  discountNode {
    metafield(namespace: "fixed-amount", key: "function-configuration") {
      value
    }
  }
  presentmentCurrencyRate
}
