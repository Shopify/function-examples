name = "{{name}}"
type = "{{extensionType}}"
version = "2"
description = "Shipping rates consolidation base script"

[configuration]
type = "object"
fields = {}

[commands]
build = "echo 'build the wasm'"