name = "{{name}}"
type = "{{extensionType}}"
api_version = "2023-04"

[build]
command = "cargo wasi build --release"
path = "target/wasm32-wasi/release/{{name | replace: " ", "-" | downcase}}.wasm"

[ui.paths]
create = "/"
details = "/"
