query Input {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          title
          expandedBundleMetafield: metafield(namespace: "bundles", key: "ExpandTestingIgnacio") {
            type
            value
          }
        }
      }
    }
  }
}
