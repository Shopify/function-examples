query Input {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
  }
  paymentMethods {
    id
    name
  }
  presentmentCurrencyRate
  paymentCustomization {
    metafield(
      namespace: "payment-customization-sample-app"
      key: "function-configuration"
    ) {
      value
    }
  }
}
