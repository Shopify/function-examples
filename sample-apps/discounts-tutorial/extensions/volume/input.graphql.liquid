query Input {
  cart {
    lines {
      quantity
      merchandise {
        ...on ProductVariant {
          id
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "{{ app_name | replace: " ", "-" | downcase }}", key: "function-config") {
      value
    }
  }
}
