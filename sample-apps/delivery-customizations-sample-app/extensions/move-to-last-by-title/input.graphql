query Input {
  cart {
    deliveryGroups {
      deliveryOptions {
        handle
        title
      }
    }
  }
  deliveryCustomization {
    metafield(namespace: "delivery-customizations-sample-app", key: "function-configuration") {
      value
    }
  }
}
