query Input {
  cart {
    deliveryGroups {
      deliveryAddress {
        provinceCode
      }
      deliveryOptions {
        handle
        title
      }
    }
  }
  deliveryCustomization {
    metafield(
      namespace: "delivery-customization"
      key: "function-configuration"
    ) {
      value
    }
  }
}
