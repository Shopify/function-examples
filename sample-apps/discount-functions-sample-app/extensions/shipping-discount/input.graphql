query Input {
  cart {
    deliveryGroups {
      selectedDeliveryOption {
        id
      }
    }
  }
  discountNode {
    metafield(namespace: "discount-functions-sample-app", key: "function-configuration") {
      value
    }
  }
}
