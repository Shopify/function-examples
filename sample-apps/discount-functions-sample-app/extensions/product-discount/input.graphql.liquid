query Input {
  merchandiseLines {
    variant {
      id
    }
  }
  discountNode {
    metafield(namespace: "{{ app_name | replace: " ", "-" | downcase }}", key: "function-configuration") {
      value
    }
  }
}
