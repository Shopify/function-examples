query Input {
  merchandiseLines {
    variant {
      id
    }
  }
  # type: HasMetafields
  functionContext {
    configuration: metafield(namespace: 'function', key: 'configuration') {
      value
    }
  }
}
