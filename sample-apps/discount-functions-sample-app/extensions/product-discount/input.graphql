query Input {
  merchandiseLines {
    variant {
      id
    }
  }
  priceRule {
    configuration: metafield(namespace: 'discount-functions-sample-app', key: 'function-configuration') {
      value
    }
  }
}
