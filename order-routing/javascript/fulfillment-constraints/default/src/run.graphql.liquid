query RunInput {
  cart {
    deliverableLines {
      id
    }
  }
  fulfillmentConstraintRule {
    metafield(namespace: "$app:fulfillment-constraints", key: "function-configuration") {
      value
    }
  }
}